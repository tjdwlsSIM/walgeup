# 색인 요청 대기열

새 글은 검색엔진이 알아서 찾아주지 않습니다. **아래 항목은 사람이 직접 처리해야
합니다.** 처리한 뒤 체크박스를 `[x]` 로 바꿔 주세요.

- 구글: [Search Console](https://search.google.com/search-console) → URL 검사 → 색인 생성 요청
- 네이버: [서치어드바이저](https://searchadvisor.naver.com) → 요청 → 웹 페이지 수집

미처리 항목이 3건 이상 쌓이면 CTO 리포트에 "색인 요청 밀림" 알림이 뜹니다.

---

<!-- notion-logger 가 아래에 항목을 추가합니다. 형식을 바꾸지 마세요 —
     CTO 가 "^- \[ \] \*\*" 패턴으로 미처리 건수를 셉니다. -->

- [ ] **/guides/2026-changes/** — 2026-08-05 · GSC "발견됨 - 현재 색인이 생성되지 않음". 크롤링 이력 없음
- [ ] **/guides/minimum-wage-2027-monthly/** — 2026-08-05 · GSC "발견됨 - 현재 색인이 생성되지 않음". 크롤링 이력 없음
- [ ] **/guides/salary-3000/** — 2026-08-05 · GSC "발견됨 - 현재 색인이 생성되지 않음". 크롤링 이력 없음
- [ ] **/guides/unemployment-2027/** — 2026-08-05 · GSC "발견됨 - 현재 색인이 생성되지 않음". 크롤링 이력 없음

> 위 4편은 가이드 목록 페이지에서만 링크되던 사실상 고립 페이지였다. 계산기 7개에
> 「함께 보면 좋은 가이드」 섹션을 추가해 인바운드 링크를 늘렸으나(2026-08-05),
> **내부 링크만으로는 이미 "발견됨" 상태에 들어간 URL 이 자동으로 빠져나오지 않는다.**
> 배포 후 Search Console 에서 4건 모두 색인 생성을 직접 요청해야 한다.
